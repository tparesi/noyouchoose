<h2>Invite Friends</h2>
<form class="invite-friends-form">
  <% users.each(function(user) { %>
    <label><%= user.escape("name") %>
      <input type="checkbox" name="friends[]" value="<%= user.escape("name") %>">
    </label>
  <% }) %>

</form>
